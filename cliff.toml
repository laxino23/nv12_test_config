[changelog]
# 1. Header template
# 1. 日志头部模板
header = """
# Changelog

All notable changes to this project will be documented in this file.
"""

# 2. Body template
# 2. 日志主体模板 (Jinja2 syntax)
# It iterates over releases -> groups -> commits
# 遍历逻辑：版本 (releases) -> 分组 (groups) -> 提交 (commits)
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | upper_first }}
    {% for commit in commits %}
        - {% if commit.scope %}*({{ commit.scope }})* {% endif %}\
            {% if commit.breaking %}[**breaking**] {% endif %}\
            {{ commit.message | upper_first }} ([{{ commit.id | truncate(length=7, end="") }}]({{ commit.id }}))\
    {% endfor %}
{% endfor %}\n
"""

# 3. Footer template
# 3. 底部模板
footer = """
"""

# Remove leading/trailing whitespace
# 去除生成内容的首尾空格
trim = true

[git]
# Parse commits strictly following Conventional Commits
# 严格解析符合 Conventional Commits 规范的提交
conventional_commits = true

# Filter out non-conventional commits (optional, set true to hide "random" commits)
# 过滤掉不符合规范的杂乱提交 (设为 true 则隐藏非规范提交)
filter_unconventional = false

# Regex parsers to group commits
# 用于分组提交的正则表达式解析器
# The "group" string determines the section header in the changelog
# "group" 字段决定了生成的日志中的章节标题
commit_parsers = [
  { message = "^feat", group = "🚀 Features" },
  { message = "^fix", group = "🐛 Bug Fixes" },
  { message = "^doc", group = "📚 Documentation" },
  { message = "^perf", group = "⚡ Performance" },
  { message = "^refactor", group = "🚜 Refactor" },
  { message = "^style", group = "🎨 Styling" },
  { message = "^test", group = "🧪 Testing" },
  { message = "^chore\\(release\\): prepare for", skip = true },
  { message = "^chore|^ci", group = "⚙️ Miscellaneous Tasks" },
  { body = ".*security", group = "🛡️ Security" },
]

# Protect breaking changes from being skipped
# 保护破坏性更新 (Breaking Changes) 不被忽略
protect_breaking_commits = false

# Filter out merge commits
# 过滤掉合并提交
filter_commits = false

# Regex for matching git tags (e.g. v1.0.0)
# 匹配 git 标签的正则
tag_pattern = "v[0-9].*"

# Sort order
# 排序方式
topo_order = false
sort_commits = "oldest"
